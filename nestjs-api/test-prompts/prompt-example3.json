{
  "prompt": "Please analyze this code:\n\n// Bad: Using var instead of const/let\nvar counter = 0;\n\n// Bad: Global function without proper scope\nfunction sayHello() {\n    // Bad: Modifying global variable\n    counter++;\n    \n    // Bad: String concatenation instead of template literal\n    return \"Hello World! Count: \" + counter;\n}\n\n// Bad: No error handling\nfunction addMessage(messages, text) {\n    // Bad: Direct array mutation\n    messages.push(text);\n    \n    // Bad: if without brackets\n    if(messages.length > 5)\n        messages.shift();\n    \n    // Bad: Console log in production\n    console.log(\"Added message\");\n}\n\n// Bad: Global array\nvar messages = [];\n\n// Usage with bad practices\ntry {\n    sayHello();\n    addMessage(messages, undefined);  // Bad: Undefined as parameter\n} catch(e) {\n    // Bad: Empty catch block\n}"
}